<div class="container text-center">
  <h1>All Tasks</h1>
</div>
<div class="container">
  <table class="table table-striped">
    <% @tasks.each do |task| %>
    <tr>
      <td><%= task.id %>: <a href="/tasks/<%= task.id %>"><%= task.title %></a></td>
    </tr>
    <% end %>
  </table>
</div>
